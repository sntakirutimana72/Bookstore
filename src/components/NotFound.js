const styles = {
  color: '#e8e8e8',
  fontSize: '4.5rem',
  textAlign: 'center',
  padding: '6rem',
};

const NotFound = () => (
  <p style={styles}>
    Page Not Found
  </p>
);

export default NotFound;
